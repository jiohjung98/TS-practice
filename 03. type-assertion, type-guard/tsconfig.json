{
    "compilerOptions": {
        "rootDir": "./src", // src 파일안에서 root 파일을 명시
        "outDir": "./build/js",   // ts 컴파일 후에 어느 경로로 들어가는지 명시
        "target": "ES2015", // 타입스크립트를 ES2015 버전의 JS로 변환
        "noEmitOnError": true,
        "module": "EsNext", // 어떤 모듈 시스템을 사용할 지
        "moduleResolution": "Node",
        "esModuleInterop": true,
        "lib": ["ESNext", "DOM"],
        // "strict": true
        "strictNullChecks": false
    },
    "include": [    // 소스 파일의 어디에서 타입스크립트 파일을 찾을 수 있는지 명시
        "src/**/*.ts"
    ], 
    "exclude": [    // 컴파일 시 제외할 목록
        "node_modules/"
    ]
}